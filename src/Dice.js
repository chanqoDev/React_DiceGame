import React, { useState } from "react";

/** A game can be any number of Dies, all with random values. */
function Dice(props) {
  return <section className="Dice"></section>;
}

export default Dice;
